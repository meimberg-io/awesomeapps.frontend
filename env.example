# Environment Variables for AwesomeApps Frontend
# Copy this to .env and configure for your environment

# =============================================================================
# REQUIRED VARIABLES - Application will not work without these
# =============================================================================

# Backend API URL (public-facing)
# Local dev: http://localhost:8202
# Docker dev: http://host.docker.internal:8202
# Production: https://api.yourdomain.com
NEXT_PUBLIC_STRAPI_BASEURL=http://localhost:8202

# Frontend URL (public-facing)
# Local dev: http://localhost:8204
# Production: https://yourdomain.com
NEXT_PUBLIC_APP_BASEURL=http://localhost:8204

# Secret token for revalidation API
# Generate strong random token for production
REVALIDATE_SECRET=your-secret-token-here

# =============================================================================
# OPTIONAL VARIABLES
# =============================================================================

# Enable/disable Matomo analytics
NEXT_PUBLIC_MATOMO_TRACKER=false

# Port for Docker development (npm dev always uses 3000)
APP_PORT=8204

# Environment
NODE_ENV=development

# =============================================================================
# IMPORTANT NOTES
# =============================================================================
# 
# 1. NEXT_PUBLIC_* variables are BUILD-TIME variables
#    - They are embedded into the static build
#    - Changing them requires: npm run build (or rebuild Docker image)
#
# 2. Production checklist:
#    - Use HTTPS URLs for NEXT_PUBLIC_* variables
#    - Generate strong random REVALIDATE_SECRET
#    - Never commit .env to git
#    - Configure Strapi CORS for your frontend URL
#
# 3. Docker notes:
#    - Docker dev: Map port with APP_PORT
#    - Docker prod: Always runs on port 5680 internally
#    - Use host.docker.internal to reach host services from container

