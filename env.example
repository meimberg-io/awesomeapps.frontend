# Environment Variables Example
# Copy this to .env for local development

# Application
NODE_ENV=development
APP_PORT=8203

# Next.js Public Variables (accessible in browser)
# For Docker: Use host.docker.internal to access host services from container
# For local npm dev: Use localhost
# For production: Use actual domain URLs
NEXT_PUBLIC_STRAPI_BASEURL=http://host.docker.internal:8202
NEXT_PUBLIC_APP_BASEURL=http://localhost:8203

# Internal API URL (server-side only, REQUIRED for production)
# Used for server-side data fetching to avoid external DNS lookups
# STRAPI_INTERNAL_URL=http://awesomeapps-strapi:1337

# Matomo Analytics (optional)
NEXT_PUBLIC_MATOMO_TRACKER=false

# Revalidation Secret for API routes (Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
REVALIDATE_SECRET=your-secret-token-here

# Admin Email Whitelist (comma-separated list of authorized admin email addresses)
# Example: ADMIN_EMAILS=admin1@example.com,admin2@example.com
# If not set, defaults to: oli@meimberg.io
# ADMIN_EMAILS=oli@meimberg.io

# Development specific
DEBUG=true
LOG_LEVEL=debug
